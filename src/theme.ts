import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  cssVariables: true,
  palette: {
    primary: {
      main: "#f69800",
    },
    secondary: {
      main: "#39B4E8",
    },
  },
});

export default theme;
